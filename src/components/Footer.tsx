export function Footer() {
  return (
    <footer className="page-footer">
      <p>
        Good for covers, cards, backgrounds, or anywhere you need
        something visual that isn't a photo.
      </p>
    </footer>
  )
}
